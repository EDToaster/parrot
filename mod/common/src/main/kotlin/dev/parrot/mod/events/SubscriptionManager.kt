package dev.parrot.mod.events

import dev.parrot.mod.engine.bridge.ScreenObservation

class SubscriptionManager {
    fun cleanupChannel(channelId: String) {}
    fun handleScreenObservation(observation: ScreenObservation) {}
}
